// Example TypeScript code

class MyClass {
    public functionMyMethod(param1: string, param2: number): void {
        console.log(`Method executed with param1: ${param1} and param2: ${param2}`);
    }
}

const myInstance = new MyClass();
myInstance.functionMyMethod("Hello", 42);
